/* -------------------------------------------------------------------
   Refract backup format 3.0. One .jsonl section per file, joined on
   mediaItemId; library is the spine. Section rows arrive unflattened as
   tables[section].raw.
   ------------------------------------------------------------------- */

export function buildV3Model() {
  throw new Error('Reading Refract 3.0 backups is not implemented yet.');
}
